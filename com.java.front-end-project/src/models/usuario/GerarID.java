package models.usuario;

public abstract class GerarID {
    /**
     * Generates a unique ID based on a key.
     *
     * @param chave the key.
     * @return the ID.
     */
    public static int gerarId(String chave) {
        // Generate ID logic...
        return 0;
    }
}
